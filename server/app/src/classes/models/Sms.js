/*
|-----------------------------------------------------------------
|	Sms
|-----------------------------------------------------------------
|
|	Sms
|
*/
let config = require('../../config.json')

class Sms {
	constructor() {

	}

	static send() {
		return new Promise((resolve,reject) => {

			return resolve()
		})
	}
}

module.exports = Sms